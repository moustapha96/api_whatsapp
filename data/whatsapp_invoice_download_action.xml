<?xml version="1.0" encoding="utf-8"?>
<!-- Action pour télécharger le PDF d'une facture -->
<odoo>
    <!-- Action pour télécharger le PDF d'une facture -->
    <record id="action_btn_download_invoice" model="whatsapp.button.action">
        <field name="name">Télécharger PDF facture</field>
        <field name="button_id">btn_download_invoice</field>
        <field name="action_type">custom_python</field>
        <field name="python_code"><![CDATA[
import base64

_logger.info("=== Début action téléchargement facture ===")
_logger.info("Button ID reçu: %s", button_id)
_logger.info("Message phone: %s", message.phone if message else 'N/A')

# Extrait l'ID de la facture depuis le button_id
invoice_id = None
invoice = None

# Le button_id est au format "btn_download_invoice_123" où 123 est l'ID de la facture
if button_id and button_id.startswith('btn_download_invoice_'):
    try:
        invoice_id_str = button_id.replace('btn_download_invoice_', '')
        invoice_id = int(invoice_id_str)
        invoice = env['account.move'].browse(invoice_id)
        if not invoice.exists():
            invoice = None
            _logger.warning("Facture avec ID %s n'existe pas (button_id: %s)", invoice_id, button_id)
        else:
            _logger.info("Facture trouvée par ID: %s (button_id: %s)", invoice.name, button_id)
    except ValueError as e:
        _logger.warning("Impossible de convertir l'ID de facture depuis button_id %s: %s", button_id, str(e))
        invoice = None
    except Exception as e:
        _logger.warning("Erreur lors de la recherche de facture par ID depuis button_id %s: %s", button_id, str(e))
        invoice = None

# Si pas trouvé par ID, cherche par numéro de téléphone du partenaire
if not invoice:
    _logger.info("Facture non trouvée par ID, recherche par numéro de téléphone: %s", message.phone)
    phone_clean = message.phone.replace(' ', '').replace('-', '').replace('.', '')
    if not phone_clean.startswith('+'):
        phone_clean = '+' + phone_clean.lstrip('+')
    
    # Cherche d'abord avec le numéro exact
    invoice = env['account.move'].search([
        ('partner_id.phone', '=', phone_clean),
        ('move_type', 'in', ['out_invoice', 'out_refund']),
        ('state', '=', 'posted')
    ], order='create_date desc', limit=1)
    
    # Si pas trouvé, cherche avec ilike (moins restrictif)
    if not invoice:
        invoice = env['account.move'].search([
            ('partner_id.phone', 'ilike', phone_clean),
            ('move_type', 'in', ['out_invoice', 'out_refund']),
            ('state', '=', 'posted')
        ], order='create_date desc', limit=1)
    
    if invoice:
        _logger.info("Facture trouvée par numéro de téléphone: %s", invoice.name)
    else:
        _logger.warning("Aucune facture trouvée pour le numéro %s", phone_clean)

if invoice:
    _logger.info("✅ Facture trouvée: %s (ID: %s), Partenaire: %s (ID: %s)", 
               invoice.name, invoice.id, invoice.partner_id.name if invoice.partner_id else 'N/A', 
               invoice.partner_id.id if invoice.partner_id else 'N/A')
    try:
        # Essaie plusieurs méthodes pour trouver le rapport
        report = None
        report_names = ['account.report_invoice', 'account.report_invoice_with_payments']
        
        for report_name in report_names:
            try:
                report = env['ir.actions.report']._get_report_from_name(report_name)
                if report and report.exists() and report.id:
                    break
                else:
                    report = None
            except:
                report = None
                continue
        
        if not report or not report.exists():
            report = env['ir.actions.report'].search([
                ('report_name', 'in', report_names),
                ('model', '=', 'account.move')
            ], limit=1)
        
        if report and report.exists():
            # Génère le PDF
            pdf_content, _unused = report._render_qweb_pdf(invoice.id)
            
            if pdf_content:
                # Crée un attachment public pour le PDF
                attachment = env['ir.attachment'].create({
                    'name': f"{invoice.name}.pdf",
                    'type': 'binary',
                    'datas': base64.b64encode(pdf_content),
                    'res_model': 'account.move',
                    'res_id': invoice.id,
                    'public': True,  # Important : rend le fichier accessible publiquement
                })
                
                # Génère l'URL publique de téléchargement
                base_url = env['ir.config_parameter'].sudo().get_param('web.base.url')
                pdf_url = f"{base_url}/web/content/{attachment.id}?download=true"
                
                # Envoie le message avec le lien de téléchargement
                config = message.config_id
                if not config:
                    # Essaie de récupérer la configuration active
                    config = env['whatsapp.config'].search([('is_active', '=', True)], limit=1)
                    _logger.info("Configuration récupérée depuis message.config_id: %s, depuis recherche: %s", 
                               message.config_id, config.id if config else None)
                
                if config:
                    download_message = f"📄 Voici le lien pour télécharger votre facture {invoice.name} :\n\n"
                    download_message += f"{pdf_url}\n\n"
                    download_message += f"Montant : {invoice.amount_total:.0f} F CFA"
                    
                    _logger.info("Tentative d'envoi du message de téléchargement pour la facture %s au partenaire %s (ID: %s)", 
                               invoice.name, invoice.partner_id.name, invoice.partner_id.id)
                    
                    try:
                        result = config.send_text_to_partner(
                            partner_id=invoice.partner_id.id,
                            message_text=download_message
                        )
                        
                        _logger.info("Résultat de l'envoi pour la facture %s: %s", invoice.name, result)
                        
                        if result and isinstance(result, dict) and result.get('success'):
                            _logger.info("✅ Lien de téléchargement envoyé avec succès pour la facture %s", invoice.name)
                            message.content = f"Lien PDF facture {invoice.name} envoyé"
                        else:
                            error_msg = result.get('error', 'Erreur inconnue') if isinstance(result, dict) else str(result)
                            _logger.warning("❌ Échec de l'envoi du lien de téléchargement pour la facture %s: %s", invoice.name, error_msg)
                            message.content = f"Échec envoi lien PDF facture {invoice.name}: {error_msg}"
                    except Exception as e:
                        _logger.exception("Erreur lors de l'envoi du message de téléchargement pour la facture %s", invoice.name)
                        message.content = f"Erreur envoi: {str(e)}"
                else:
                    _logger.error("❌ Configuration WhatsApp non trouvée pour envoyer le lien de téléchargement (message.config_id: %s)", 
                                message.config_id.id if message.config_id else None)
                    message.content = "Configuration WhatsApp non trouvée"
            else:
                _logger.warning("Impossible de générer le PDF pour la facture %s", invoice.name)
                message.content = f"Erreur génération PDF facture {invoice.name}"
        else:
            _logger.warning("Aucun rapport trouvé pour générer le PDF de la facture %s", invoice.name)
            message.content = f"Rapport PDF non trouvé pour facture {invoice.name}"
    except Exception as e:
        _logger.error("Erreur lors de la génération et envoi du PDF pour la facture %s: %s", invoice.name, str(e))
        message.content = f"Erreur génération PDF: {str(e)}"
else:
    _logger.warning("❌ Aucune facture trouvée pour téléchargement via WhatsApp - Bouton: %s, Numéro: %s", button_id, message.phone if message else 'N/A')
    message.content = "Aucune facture trouvée pour téléchargement"

_logger.info("=== Fin action téléchargement facture ===")
]]></field>
        <field name="sequence">10</field>
        <field name="active">True</field>
        <field name="description">Envoie le lien de téléchargement du PDF d'une facture lorsque le client clique sur "Télécharger PDF"</field>
    </record>
</odoo>

